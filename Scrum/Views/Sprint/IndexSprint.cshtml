@model IEnumerable<Scrum.Models.SprintViewModel.SprintViewModel>

@{
    ViewBag.Title = "IndexSprint";
}

<h2>IndexSprint</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjectID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MainTask)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MainTask)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SprintID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SprintID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SprintID })
        </td>
    </tr>
}

</table>
